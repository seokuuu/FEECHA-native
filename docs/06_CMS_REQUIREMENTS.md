# FEE-CHA CMS 요구사항

## 개요

**FEE-CHA CMS**는 관리자가 플랫폼을 운영하기 위한 웹 기반 대시보드입니다.

---

## 기술 스택

### 프론트엔드
- **프레임워크**: Next.js 15 (App Router)
- **언어**: TypeScript 5.3+
- **스타일링**: Tailwind CSS 3.x
- **UI 라이브러리**: shadcn/ui
- **상태 관리**: Zustand 4.x
- **데이터 페칭**: TanStack Query v5
- **차트**: Recharts
- **폼 관리**: React Hook Form + Zod
- **테이블**: TanStack Table
- **인증**: JWT (API와 동일)

### 배포
- **호스팅**: Vercel
- **도메인**: `cms.fee-cha.com`

---

## 목차

1. [인증 및 권한](#1-인증-및-권한)
2. [대시보드](#2-대시보드)
3. [사용자 관리](#3-사용자-관리)
4. [사장님 인증 관리](#4-사장님-인증-관리)
5. [거래 관리](#5-거래-관리)
6. [정산 관리](#6-정산-관리)
7. [후기 관리](#7-후기-관리)
8. [게시판 관리](#8-게시판-관리)
9. [공지사항 및 약관](#9-공지사항-및-약관)
10. [시스템 설정](#10-시스템-설정)
11. [통계 및 리포트](#11-통계-및-리포트)
12. [알림 시스템](#12-알림-시스템)

---

## 1. 인증 및 권한

### 1.1 로그인

**URL**: `/login`

**UI 구성**:
```
┌─────────────────────────────────────┐
│                                     │
│        FEE-CHA 관리자              │
│                                     │
│   [이메일]                         │
│   [비밀번호]                       │
│                                     │
│   [로그인]                         │
│                                     │
└─────────────────────────────────────┘
```

**기능**:
- 이메일/비밀번호 로그인
- JWT 토큰 발급 (API와 동일)
- 2FA (선택적, 나중에 추가)

### 1.2 권한 관리

**Role**: `ADMIN` 만 CMS 접근 가능

**Permission** (향후 확장):
- `admin.users.read`
- `admin.users.manage`
- `admin.settlements.read`
- `admin.settlements.process`
- 등등...

---

## 2. 대시보드

**URL**: `/dashboard`

### 2.1 주요 지표 카드

```
┌─────────┬─────────┬─────────┬─────────┐
│ 총 거래액│ 거래 건수│ 신규 가입│ 활성 사장│
│ 5천만원 │  200건  │  50명   │  30명   │
│ ↑ 15%   │ ↑ 10%   │ ↑ 20%   │ ↑ 5%    │
└─────────┴─────────┴─────────┴─────────┘
```

**표시 항목**:
- 총 거래액 (GMV) - 이번 달/지난 달 비교
- 거래 건수 - 이번 달/지난 달 비교
- 신규 가입자 수 - 이번 달/지난 달
- 활성 사장님 수 - 이번 달 거래한 사장님

### 2.2 매출 추이 그래프

**Line Chart**:
- X축: 날짜 (일별/월별 전환 가능)
- Y축: 매출액
- 기간 선택: 최근 7일, 30일, 3개월, 1년

### 2.3 지역별 거래 분포

**Bar Chart**:
- 서울, 경기, 인천, 기타
- 거래 건수 또는 매출액

### 2.4 인기 옵션 통계

**Pie Chart**:
- 커피 only vs 도시락 포함 vs 현수막 포함 등

### 2.5 최근 활동

**테이블**:
| 시간 | 유형 | 내용 | 상태 |
|------|------|------|------|
| 5분 전 | 신규 가입 | 홍길동님이 가입 | - |
| 10분 전 | 거래 완료 | OO커피차 예약 완료 | 완료 |
| 30분 전 | 인증 요청 | XX커피 인증 대기 | 대기 |

---

## 3. 사용자 관리

**URL**: `/users`

### 3.1 사용자 목록

**필터**:
- 역할: 전체 / 클라이언트 / 사장님
- 상태: 전체 / 활성 / 정지 / 탈퇴
- 검색: 이름, 이메일, 전화번호

**테이블**:
| ID | 이름 | 이메일 | 역할 | 가입일 | 상태 | 액션 |
|----|------|--------|------|--------|------|------|
| uuid | 홍길동 | hong@... | CLIENT | 2025-01-01 | 활성 | [상세] [정지] |

**페이지네이션**: 20개씩

### 3.2 사용자 상세

**URL**: `/users/:id`

**탭 구성**:
1. **기본 정보**
   - 이름, 이메일, 전화번호
   - 가입일, 마지막 로그인
   - 프로필 이미지

2. **활동 내역**
   - 의뢰 목록 (클라이언트)
   - 견적 목록 (사장님)
   - 거래 내역

3. **후기 내역**
   - 작성한 후기 (클라이언트)
   - 받은 후기 (사장님)

4. **신고 내역**
   - 신고한 내역
   - 신고당한 내역

**액션**:
- [사용자 정지] 버튼
  - 정지 사유 입력
  - 정지 기간 선택 (7일, 30일, 영구)
- [사용자 복구] 버튼
- [강제 탈퇴] 버튼

---

## 4. 사장님 인증 관리

**URL**: `/certifications`

### 4.1 인증 대기 목록

**필터**:
- 상태: 대기 / 승인 / 거절
- 정렬: 신청일순, 이름순

**카드 레이아웃**:
```
┌───────────────────────────────────────┐
│ OO커피차                              │
│ 신청일: 2025-01-01                    │
│ 사업자번호: 123-45-67890             │
│ 지역: 서울 전역                       │
│                                       │
│ [상세 보기] [승인] [거절]            │
└───────────────────────────────────────┘
```

### 4.2 인증 상세

**URL**: `/certifications/:id`

**섹션 구성**:

1. **사장님 기본 정보**
   - 상호명
   - 사업자등록번호
   - 대표자명
   - 연락처

2. **인증 서류** (이미지 뷰어)
   - 사업자등록증 (확대 가능)
   - 영업신고증
   - 위생교육증
   - 차량 사진 (정면, 측면, 내부)

3. **심사 액션**
   - [승인하기] 버튼 → 즉시 승인
   - [거절하기] 버튼 → 거절 사유 입력 모달

**승인 시**:
- 사장님에게 푸시 알림
- 슬랙 알림 (선택적)

**거절 시**:
- 거절 사유 입력 (필수)
- 사장님에게 푸시 알림 + 이메일

---

## 5. 거래 관리

**URL**: `/bookings`

### 5.1 거래 목록

**필터**:
- 상태: 전체 / 대기 / 확정 / 완료 / 취소
- 날짜: 이벤트 날짜 범위
- 검색: 클라이언트명, 사장님명, 장소

**테이블**:
| ID | 클라이언트 | 사장님 | 이벤트 날짜 | 금액 | 상태 | 액션 |
|----|-----------|--------|------------|------|------|------|
| uuid | 홍길동 | OO커피차 | 2025-12-31 | 256만원 | 확정 | [상세] |

### 5.2 거래 상세

**URL**: `/bookings/:id`

**섹션 구성**:

1. **거래 정보**
   - 예약 번호
   - 이벤트 날짜, 시간, 장소
   - 예상 인원

2. **클라이언트 정보**
   - 이름, 연락처
   - 의뢰 내용

3. **사장님 정보**
   - 상호명, 연락처
   - 제출한 견적

4. **금액 정보**
   - 총액
   - 예약금 (결제 상태)
   - 잔금 (결제 상태)
   - 플랫폼 수수료

5. **채팅 로그**
   - 클라이언트 ↔ 사장님 채팅 내역 조회
   - 분쟁 시 증거 자료

**액션**:
- [강제 취소] 버튼 (분쟁 해결용)
- [환불 처리] 버튼

---

## 6. 정산 관리

**URL**: `/settlements`

### 6.1 정산 대기 목록

**필터**:
- 상태: 대기 / 처리 중 / 완료 / 실패
- 정산 예정일: 날짜 범위
- 사장님 검색

**테이블**:
| 사장님 | 거래 건수 | 정산 금액 | 수수료 | 정산 예정일 | 상태 | 액션 |
|--------|----------|----------|--------|------------|------|------|
| OO커피차 | 5건 | 400만원 | 40만원 | 2025-01-08 | 대기 | [선택] |

**일괄 정산 버튼**:
- 선택된 항목들을 한번에 정산 처리
- 확인 모달: "총 10건, 5,000만원을 정산하시겠습니까?"

### 6.2 정산 상세

**URL**: `/settlements/:id`

**섹션 구성**:

1. **사장님 정보**
   - 상호명
   - 계좌 정보 (은행, 계좌번호, 예금주)

2. **정산 내역**
   | 거래 ID | 이벤트 날짜 | 총액 | 수수료 | 정산액 |
   |--------|------------|------|--------|--------|
   | uuid | 2025-12-31 | 256만원 | 25만원 | 231만원 |

3. **합계**
   - 총 거래액: 5,000만원
   - 총 수수료: 500만원
   - 총 정산액: 4,500만원

**액션**:
- [정산 처리] 버튼
- [정산 보류] 버튼 (이유 입력)

### 6.3 정산 내역 (완료)

**URL**: `/settlements/history`

**엑셀 다운로드** 버튼:
- 기간 선택
- 사장님별 / 월별 정산 내역 엑셀 다운로드

---

## 7. 후기 관리

**URL**: `/reviews`

### 7.1 후기 목록

**필터**:
- 별점: 전체 / 5점 / 4점 / 3점 / 2점 / 1점
- 신고 여부: 전체 / 신고됨
- 상태: 전체 / 공개 / 숨김 / 삭제
- 검색: 내용, 작성자

**테이블**:
| 작성자 | 사장님 | 별점 | 내용 미리보기 | 신고 | 상태 | 액션 |
|--------|--------|------|--------------|------|------|------|
| 홍길동 | OO커피차 | ⭐⭐⭐⭐⭐ | 정말 만족... | 0건 | 공개 | [상세] |

### 7.2 후기 상세

**URL**: `/reviews/:id`

**섹션 구성**:

1. **후기 정보**
   - 작성자, 사장님
   - 별점 (전체 + 세부 항목)
   - 내용
   - 사진

2. **거래 정보**
   - 예약 번호
   - 이벤트 날짜

3. **신고 내역** (있을 경우)
   - 신고자
   - 신고 사유
   - 신고 일시

**액션**:
- [숨김 처리] 버튼
- [삭제] 버튼
- [복구] 버튼

---

## 8. 게시판 관리

### 8.1 사장님 프로필 관리

**URL**: `/vendor-profiles`

**기능**:
- 부적절한 내용 블라인드
- 허위 정보 삭제
- 프로필 승인/거절 (인증과 별도)

### 8.2 의뢰 게시판 관리

**URL**: `/requests`

**기능**:
- 스팸 의뢰 삭제
- 부적절한 내용 필터링

---

## 9. 공지사항 및 약관

### 9.1 공지사항

**URL**: `/notices`

**목록**:
| 제목 | 작성일 | 노출 | 푸시 발송 | 액션 |
|------|--------|------|----------|------|
| 설 연휴 안내 | 2025-01-20 | 공개 | ✅ | [수정] [삭제] |

**작성/수정**:
- 제목
- 내용 (Rich Text Editor)
- 푸시 알림 발송 여부
- 팝업 노출 여부

### 9.2 약관 관리

**URL**: `/terms`

**종류**:
- 이용약관
- 개인정보 처리방침
- 위치기반 서비스 이용약관

**버전 관리**:
| 버전 | 제목 | 적용일 | 상태 | 액션 |
|------|------|--------|------|------|
| v1.1 | 이용약관 | 2025-01-01 | 적용 중 | [보기] [복사] |
| v1.0 | 이용약관 | 2024-01-01 | 이전 버전 | [보기] |

**작성/수정**:
- 제목
- 내용 (Rich Text Editor)
- 적용 예정일
- 필수 동의 / 선택 동의

---

## 10. 시스템 설정

**URL**: `/settings`

### 10.1 수수료 설정

**항목**:
- 직접 선택 수수료: [ 5 ] %
- 견적 비교 수수료: [ 10 ] %
- 부가세 포함 여부: [✅]

### 10.2 결제 설정

**항목**:
- 포트원 API 키: `imp_*******`
- 포트원 API Secret: `********`
- 테스트 모드: [  ] 활성화

### 10.3 알림 설정

**항목**:
- FCM Server Key: `********`
- 슬랙 웹훅 URL: `https://hooks.slack.com/...`
- 이메일 발송 서비스: SendGrid / AWS SES

### 10.4 지역 관리

**지역 목록**:
- 서울 전역
- 경기 북부
- 경기 남부
- 인천
- 강원도
- ...

**출장비 기준**:
| 거리 | 출장비 |
|------|--------|
| 0-10km | 0원 |
| 11-30km | 3만원 |
| 31-50km | 5만원 |
| 51km 이상 | 협의 |

---

## 11. 통계 및 리포트

**URL**: `/statistics`

### 11.1 매출 통계

**차트**:
- 일별/주별/월별 매출 추이
- 지역별 매출 분포
- 사장님별 매출 순위

**필터**:
- 기간: 날짜 범위 선택
- 지역: 전체 / 특정 지역
- 사장님: 전체 / 특정 사장님

### 11.2 사용자 통계

**차트**:
- 신규 가입 추이
- 활성 사용자 수 (MAU, DAU)
- 이탈 사용자 분석

### 11.3 거래 통계

**지표**:
- 평균 거래액
- 인기 옵션 순위
- 취소율
- 재거래율

### 11.4 데이터 내보내기

**엑셀 다운로드**:
- 사용자 목록
- 거래 내역
- 정산 내역
- 리뷰 목록

**기간 선택** 및 **필터 적용** 가능

---

## 12. 알림 시스템

### 12.1 관리자 알림

**슬랙 연동**:
- 새 사장님 인증 요청 → 슬랙 채널 알림
- 신고 접수 → 슬랙 채널 알림
- 정산 처리 실패 → 슬랙 채널 알림

### 12.2 웹 푸시 알림

**브라우저 알림**:
- CMS 접속 시 브라우저 알림 권한 요청
- 중요한 이벤트 발생 시 브라우저 알림

---

## UI/UX 가이드라인

### 레이아웃

```
┌─────────────────────────────────────────────┐
│ [로고] FEE-CHA CMS          [👤 관리자] [🔔] │
├──────┬──────────────────────────────────────┤
│ 메뉴 │                                      │
│      │      Main Content Area               │
│ 대시보드│                                      │
│ 사용자│                                      │
│ 인증  │                                      │
│ 거래  │                                      │
│ 정산  │                                      │
│ 후기  │                                      │
│ 통계  │                                      │
│ 설정  │                                      │
│      │                                      │
└──────┴──────────────────────────────────────┘
```

### 컬러 테마

```typescript
// @fee-cha/ui-config 사용
import { colors } from '@fee-cha/ui-config';

// Primary: 오렌지 (브랜드 색상)
// Secondary: 블루
// Success: 그린
// Error: 레드
// Gray: 그레이스케일
```

### shadcn/ui 컴포넌트

**사용할 컴포넌트**:
- `Button`
- `Card`
- `Table`
- `Dialog` (Modal)
- `Select`
- `Input`
- `Checkbox`
- `Tabs`
- `Badge`
- `Avatar`
- `Calendar`
- `DropdownMenu`
- `Sheet` (Sidebar)
- `Toast`

### 반응형 디자인

- **Desktop First**: 관리자는 주로 데스크톱 사용
- **Tablet 지원**: 가로 레이아웃 유지
- **Mobile**: 간단한 조회만 가능 (정산 처리 등은 데스크톱 권장)

---

## 개발 우선순위

### Phase 1: 필수 기능 (MVP)
1. 인증 및 로그인
2. 대시보드 (기본 통계)
3. 사용자 목록 및 상세
4. 사장님 인증 관리
5. 거래 목록 및 상세
6. 정산 목록 및 처리

### Phase 2: 핵심 기능
1. 후기 관리
2. 공지사항 관리
3. 시스템 설정
4. 슬랙 알림 연동

### Phase 3: 고도화
1. 통계 및 리포트
2. 데이터 엑셀 다운로드
3. 약관 버전 관리
4. 고급 필터링

---

## 기술 구현 상세

### Next.js App Router 구조

```
apps/cms/
├── src/
│   ├── app/
│   │   ├── (auth)/
│   │   │   └── login/
│   │   │       └── page.tsx
│   │   ├── (dashboard)/
│   │   │   ├── layout.tsx         # 사이드바 레이아웃
│   │   │   ├── page.tsx           # 대시보드
│   │   │   ├── users/
│   │   │   │   ├── page.tsx       # 사용자 목록
│   │   │   │   └── [id]/
│   │   │   │       └── page.tsx   # 사용자 상세
│   │   │   ├── certifications/
│   │   │   ├── bookings/
│   │   │   ├── settlements/
│   │   │   ├── reviews/
│   │   │   ├── notices/
│   │   │   ├── settings/
│   │   │   └── statistics/
│   │   └── api/
│   │       └── auth/
│   │           └── route.ts
│   ├── components/
│   │   ├── ui/                    # shadcn/ui 컴포넌트
│   │   ├── layout/
│   │   │   ├── Sidebar.tsx
│   │   │   ├── Header.tsx
│   │   │   └── Footer.tsx
│   │   ├── dashboard/
│   │   ├── users/
│   │   └── ...
│   ├── lib/
│   │   ├── api.ts                 # Axios 인스턴스
│   │   ├── utils.ts
│   │   └── auth.ts
│   ├── hooks/
│   │   ├── useAuth.ts
│   │   ├── useUsers.ts
│   │   └── ...
│   └── stores/
│       ├── authStore.ts
│       └── ...
├── public/
├── tailwind.config.ts
└── package.json
```

### API 호출

```typescript
// lib/api.ts
import axios from 'axios';

const api = axios.create({
  baseURL: process.env.NEXT_PUBLIC_API_URL,
});

// 요청 인터셉터 (토큰 자동 추가)
api.interceptors.request.use((config) => {
  const token = localStorage.getItem('accessToken');
  if (token) {
    config.headers.Authorization = `Bearer ${token}`;
  }
  return config;
});

// 응답 인터셉터 (401 처리)
api.interceptors.response.use(
  (response) => response,
  (error) => {
    if (error.response?.status === 401) {
      // 로그인 페이지로 리다이렉트
      window.location.href = '/login';
    }
    return Promise.reject(error);
  }
);

export default api;
```

### TanStack Query 사용

```typescript
// hooks/useUsers.ts
import { useQuery, useMutation } from '@tanstack/react-query';
import api from '@/lib/api';

export function useUsers(params?: UsersQueryParams) {
  return useQuery({
    queryKey: ['users', params],
    queryFn: async () => {
      const { data } = await api.get('/admin/users', { params });
      return data;
    },
  });
}

export function useSuspendUser() {
  return useMutation({
    mutationFn: async ({ userId, reason, duration }: SuspendUserParams) => {
      const { data } = await api.post(`/admin/users/${userId}/suspend`, {
        reason,
        duration,
      });
      return data;
    },
  });
}
```

---

## 배포 설정

### Vercel 환경 변수

```env
NEXT_PUBLIC_API_URL=https://api.fee-cha.com/v1
NEXT_PUBLIC_SOCKET_URL=wss://api.fee-cha.com
```

### 빌드 설정

```json
{
  "scripts": {
    "dev": "next dev",
    "build": "next build",
    "start": "next start",
    "lint": "next lint"
  }
}
```

---

## 보안 고려사항

### 인증
- JWT 토큰 만료 시 자동 로그아웃
- Refresh Token 사용 (선택적)
- CSRF 방지

### 권한
- 모든 API 요청에 `ADMIN` 역할 확인
- 민감한 액션 (정산 처리, 사용자 삭제 등) 재확인 모달

### 데이터 보호
- 개인정보는 최소한으로 노출
- 비밀번호, 계좌번호 등은 마스킹 처리

---

## 관련 문서

- [백엔드 요구사항](./03_BACKEND_REQUIREMENTS.md)
- [API 명세서](./04_API_SPECS.md)
- [배포 가이드](./07_DEPLOYMENT.md)
